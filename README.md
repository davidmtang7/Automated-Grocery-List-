# Automated-Grocery-List-
Working on an app that makes a grocery list from user inputted recipes, 
aiming to provide as much variety as possible while also staying under a budget.

Setup:
git clone <your-repo>
cd "Grocery List App"
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
